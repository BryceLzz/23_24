import requests
import gettext
import locale

# Configurar el directorio de traducciones
locale_dir = 'locales'
gettext.bindtextdomain('CocktailApp', locale_dir)
gettext.textdomain('CocktailApp')
lang = locale.getdefaultlocale()[0]
lang = 'es' if lang.startswith('es') else 'en'
_ = gettext.gettext

class TheCocktailDBAPI:
    # Resto del código...

    @staticmethod
    def search_cocktails(search_term):
        # Resto del código...
        return drinks  # Devuelve los datos brutos de los cócteles
    return []

    @staticmethod
    def search_ingredients(search_term):
        # Resto del código...
        return data["ingredients"]  # Devuelve los datos brutos de los ingredientes
    return []

    # Resto del código...
